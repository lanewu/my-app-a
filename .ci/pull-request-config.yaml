version: "2.1"

dependencies: ./project-dependencies.yaml

default:
  build-command:
    before:
      current: echo $(ls -l ~/.m2/repository/com/mycompany/app/)
      upstream: echo $(ls -l ~/.m2/repository/com/mycompany/app/)
    current: ｜
      mvn -e clean install
    upstream: ｜
      mvn -e -T1C clean install -DskipTests
    after:
      upstream: rm -rf ./*
